#target Illustrator// Removes groupd by checking for same x/y and h/w// known issues: maybe does weird shit with textPaths?var doc, pathRef,pathMatch;if ( app.documents.length > 0 && app.activeDocument.pathItems.length > 0 ) {    doc = app.activeDocument;    for (var i = 0; i < doc.pathItems.length; i++ ) {        pathRef = doc.pathItems[i];                for (var j = 0; j < doc.pathItems.length; j++) {            if (i != j) {                pathMatch = doc.pathItems[j];                if (pathRef.top == pathMatch.top && pathRef.left == pathMatch.left && pathRef.width == pathMatch.width && pathRef.height == pathMatch.height) {                    pathMatch.remove();                }            }                    }        //~         pathRef.strokeColor = b; // for debugging    }}