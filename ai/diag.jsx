/* settings */var dist = 5;/* begin main script */var doc,    newPath;if ( app.documents.length == 0 ) {    doc = app.documents.add();} else {    doc = app.activeDocument;}var docW = doc.width,    docH = doc.height,    artLayer = doc.layers[0];    if(docW >= docH) {    for (var i = 1; i <= docW*2; i+=dist) {        newPath = doc.pathItems.add();        newPath.setEntirePath([[i,0],[0,-i]]);    }} else {    /*TODO: test this*/    for (var i = 1; i <= docH; i+=dist) {        newPath = doc.pathItems.add();        newPath.setEntirePath([[0,i],[i,0]]);    }}