if ( app.documents.length == 0 ) {    doc = app.documents.add();} else {    var doc = app.activeDocument;    // or     doc = documents[0];}//get width and height, and get the layersvar docW = doc.width,    docH = doc.height,    artLayer = doc.layers[0];for (x = 0; x < docW; x += (docW/100) ) {    for(y = 0; y < docH; y += (docH/100) ) {        r = getRandomRadius(5,15);        var ellipse = artLayer.pathItems.ellipse((x-docW), y, r, r);    }}function getRandomRadius(min,max) {    return Math.random() * (max - min) + min;}