#target illustrator// remove every other pathdoc = app.activeDocument;if ( app.documents.length > 0 && doc.pathItems.length > 0 ) {        $.writeln("before: " + doc.pathItems.length);    for (var i = doc.pathItems.length-1; i >= 0; i-- ) {        pathRef = doc.pathItems[i];        if (i%2 == 0) pathRef.remove();    }    $.writeln("after: " + doc.pathItems.length);}