if ( app.documents.length == 0 ) {    doc = app.documents.add();} else {    var doc = app.activeDocument;    // or     doc = documents[0];}var fontCount = textFonts.length;for(var i=0; i<fontCount; i++) {        //textFonts contain all of the fonts available to Illustrator    fontItem = textFonts[i];        //this gives you the family name (i.e. Apex New)    $.writeln(fontItem.family);        //this gives you the font’s full name (i.e. ApexNew-BookItalic)    $.writeln(fontItem.name);        //this gives you the font’s style name (i.e. Bold Italic, Bold, Italic, etc.)    $.writeln(fontItem.style);        $.writeln(fontItem.typename);    //this should alwats return TextFont}//get a font by its name// this name must match textFont[i].name exactlyvar arial = textFonts.getByName("Arial-Black");$.writeln(arial.name);