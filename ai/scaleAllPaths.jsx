#target Illustrator// Changes scale of all items on artboard by change rate (positive is bigger, negative is smaller)var change = -4;var scale;var b = new RGBColor;b.red = 0;b.blue = 255;b.green = 0;if ( app.documents.length > 0 && app.activeDocument.pathItems.length > 0 ) {    doc = app.activeDocument;    for (var i = 0; i < doc.pathItems.length; i++ ) {        pathRef = doc.pathItems[i];                scale = (pathRef.height+change)/pathRef.height * 100;        pathRef.resize(            scale , // x            scale , // y            true, // changePositions            true, // changeFillPatterns            true, // changeFillGradients            true, // changeStrokePattern            scale , // changeLineWidths            Transformation.CENTER); // scaleAbout        //~         pathRef.strokeColor = b; // for debugging    }}