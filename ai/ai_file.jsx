//File.openDialog ([prompt:"", filter, multiSelect:false]);var sourceFile = File.openDialog("Open your source file",null,false);//even with .openDialog, you need to convert it to a file objectvar file = File(sourceFile);// returns URI$.writeln(file.absoluteURI);// returns alias, if it exists$.writeln(file.alias);// returns creation date$.writeln(file.created);// returns creator name (probably "????")$.writeln(file.creator);// returns File name (no %20s, no extension)$.writeln(file.displayName);// returns whether file exists$.writeln(file.exists);// returns file name (keeps %20s, no extension)$.writeln(file.name);// returns file path (no filename)$.writeln(file.path);