/* settings */var dist = 2;/* begin main script */var doc,    newPath;if ( app.documents.length == 0 ) {    doc = app.documents.add();} else {    doc = app.activeDocument;}var docW = doc.width,    docH = doc.height,    artLayer = doc.layers[0];    //~ for(var w = 0; w <= docW; w+=dist) {//~     newPath = doc.pathItems.add();//~     newPath.setEntirePath([[w,0],[w,docH]]);//~ }for(var h = 0; h <= docH; h+=dist) {    newPath = doc.pathItems.add();    newPath.setEntirePath([[0,h],[docW,h]]);}